import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  middle_container: {
    alignItems: 'center',
    justifyContent: 'center',
    marginVertical: 50,
  },

  image: {
    width: 359,
    height: 362,
    resizeMode: 'contain',
  },
});
